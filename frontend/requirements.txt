# Frontend requirements for LUCA Flask Frontend
flask>=2.3.0
flask-cors>=4.0.0
aiohttp>=3.8.0

# Core dependencies (should be already installed)
# These are inherited from the main project requirements
# neo4j
# langchain
# openai